#include "arp/renderer.h"
#include "arp/math.h"
#include <algorithm>

void Renderer::drawTriangle(Vec3 v1, Vec3 v2, Vec3 v3) {


    // for(int y = std::min({v1.y, v2.y, v3.y}); y <= std::max({v1.y, v2.y, v3.y}); y++) {
    //     for(int x = std::min({v1.x, v2.x, v3.x}); x <= std::max({v1.x, v2.x, v3.x}); x++) {

    //     }
    // }
}
